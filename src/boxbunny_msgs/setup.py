from setuptools import setup

package_name = 'boxbunny_msgs'

setup(
    name=package_name,
    version='1.0.0',
    packages=[],
    data_files=[],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='boxbunny',
    maintainer_email='boxbunny@todo.todo',
    description='Custom message and service definitions for BoxBunny boxing training robot',
    license='MIT',
)
